all:
  children:
    box:
      hosts:
        sandbox-box:
          ansible_connection: community.docker.docker
          ansible_host: msp-sandbox
          site: sandbox
          msp_hc_base: http://127.0.0.1:8099
          msp_healthchecks: {heartbeat: hc-heartbeat, systemd: hc-systemd, disk: hc-disk, timers: hc-timers, _checker: hc-checker}
