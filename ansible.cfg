[defaults]
roles_path = roles
log_path = runs/ansible.log
inventory = inventory/hosts.yml
host_key_checking = True
interpreter_python = auto_silent
retry_files_enabled = False
callback_result_format = yaml
forks = 10

[ssh_connection]
pipelining = True
